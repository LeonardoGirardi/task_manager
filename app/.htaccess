RewriteEngine On

# Serve static files (CSS, JS, images) diretamente
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Serve diretórios diretamente
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Tudo o mais vai para o index.php
RewriteRule ^ index.php [QSA,L]